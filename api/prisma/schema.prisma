generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Templates {
  id  String @id @unique @default(uuid())
  url String

  @@map("templates")
}
